#include <stdio.h>

int main(void) {
   int amountToChange;
   int numFives;
   int numOnes;

   scanf("%d", &amountToChange);
   numFives = amountToChange / 5;

   numOnes =  amountToChange % 5;

   printf("numFives: %d\n", numFives);
   printf("numOnes: %d\n", numOnes);

   return 0;
}
