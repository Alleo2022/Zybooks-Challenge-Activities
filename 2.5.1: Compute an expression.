#include <stdio.h>

int main(void) {
   int num1;
   int num2;
   int finalResult;

   scanf("%d", &num1);
   scanf("%d", &num2);

   finalResult = (num1 + num2) /3;

   printf("Final result: %d\n", finalResult);

   return 0;
}
