#include <stdio.h>

int main(void) {
   int userAge;

   scanf("%d", &userAge);

   if ((userAge >= 18) && (userAge <= 25)) {
       printf("Eligible\n");
   }
   else {
      printf("Ineligible\n");
   }

   return 0;
}
