#include <stdio.h>

int main(void) {
   int userNum;
   int i;
   int j;

   scanf("%d", &userNum);

   for(int i = 0; i < userNum+1; i++){
      printf("%d\n", i);
      if(i < userNum){
      for(int j = 0; j < i+1; j++){
         printf(" ");
      }
      }
   }

   return 0;
}
